import std.io.*;

fn main() {
    let a = math.power(2, 4);

    my_file.sub_mod.say_hi();
    println("2 ^ 4 is: ", a);

    import math.power;
    println(power(-2, 4));

    my_file.sub_mod.say_hi();

    let len = std.string.length("Hello");
    println(len);
}
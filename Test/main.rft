import std.io.*;

fn main() {
    let a = {
        let a = 20;
        let b = 30;

        a + b
    };

    println(a);

    println(my_file.sub_mod.sum(a, 10));

    print("Enter your name: ");
    let name = std.io.input();
    
    import my_file.sub_mod;
    sub_mod.say_hi(name);

    test();
}

fn test() {
    println(sub_mod.sum(10, 20));
    input();
}
